# 外食費用・予算管理ツール【無料】簡単・無料計算 - 技術仕様書

## 概要

**サービス名**: Restaurant Cost Estimator
**バージョン**: 1.6.0
**更新日**: 2026-06-14
**URL**: https://appadaycreator.com/restaurant-cost-estimator/

外食回数・単価から月間外食費を計算。自炊との比較と節約額シミュレーションも。登録不要・完全無料でご利用いただけます。

## v1.6.0 更新内容（2026-06-14）

### M10 実装：印刷・結果エクスポート機能

#### 1. 結果テキストのクリップボードコピー
- 「📋 コピー」ボタンで計算結果をテキスト形式でコピー
- クリップボード機能で外部ツール・メモへの転記が容易

#### 2. CSV ダウンロード
- 「📥 CSV」ボタンで結果を CSV ファイルで保存
- 1回あたりの外食費・月間外食費・年間外食費・節約額を収録
- Excel・スプレッドシートで追加分析が可能

#### 3. 印刷機能
- 「🖨️ 印刷」ボタンで結果をブラウザ印刷メニューで出力
- 印刷 CSS で不要な要素（ボタン・広告・ナビゲーション）を自動非表示
- 結果・グラフは印刷時に表示

### M14 実装：SEO スキーマ拡張（FAQPage）

#### 1. FAQ スキーマの充実
- 5 つの Question 要素を備えた FAQPage スキーマを実装
- 各質問に詳細な Answer テキストを収録
- HTMLセクションと JSON-LDスキーマの内容が同期

#### 2. FAQ の内容カバー
- 無料・登録不要
- モバイル対応
- 外食費の目安
- コンビニ・弁当の分類方法
- 外食節約のコツ

## v1.5.0 更新内容（2026-06-14）

### M4 実装：グラフ・スコア・ゲージで結果を可視化

#### 1. Chart.js でグラフ表示
- Chart.js を CDN から導入（4.4.0）
- 複数パターン比較時に棒グラフで可視化
- マウスホバーで月間費用を表示
- レスポンシブ対応

#### 2. 外食費スコア計算・ゲージ表示
- 月間費用から 1-10 スコアを自動計算
- スコアレンジ：月2万円以下は9点、月5万円以上は1点
- プログレスバーで視覚的に表示
- スコアに応じた診断テキストを表示

### M6 実装：目標設定・達成度トラッキング

#### 1. 目標月間外食費の設定
- 入力フィールドで目標値を設定
- localStorage に自動保存・復元

#### 2. 達成度自動計算・表示
- 現在額 / 目標値 で達成度を % で表示
- プログレスバーで目標内・超過を視覚化
- 達成度に応じた励ましテキストを表示

#### 3. 前回比較
- 前回の外食費をlocalStorageで記録
- 増加・減少を矢印で表示
- 金額差を表示

### M8 実装：ツールチップ・入力ガイダンス充実

#### 1. ? アイコン + ツールチップ
- 各入力フィールドに ? アイコンを追加
- ホバーで説明文を表示

#### 2. 入力例・単位の明示
- 各入力フィールドに入力ガイド（💡 アイコン付き）を表示
- 「例：ラーメン1杯1000円」など具体例を記載
- title 属性で単位を明示

## v1.4.0 更新内容（2026-06-05）

### P1 実装済み

#### 1. グラフ表示の可視化強化
- 複数パターン比較時、パターンごとに色分けして表示
- 各パターン左側に色分けバーを追加（視認性向上）
- グラフの高さを年間費用比率で計算（より直感的な比較を実現）

#### 2. 複数パターンの年間費用表示
- パターンボックスに「月間 / 年間」両方の費用を表示
- 月間費用（大きく表示）と年間費用（小さく表示）を明確に区別
- グラフバーも年間費用に基づいて動的に更新

### P2 実装済み

#### 3. 総費用の年間金額も強調表示
- patterns-summaryセクションで月間合計と年間合計を同時表示
- 月間合計（緑色）は従来通り、年間合計（紫色）を大きく表示
- 複数パターン比較時の全体像が一目瞭然

#### 4. リセット時にパターンもクリア
- リセットボタン押下で基本入力とパターンデータを同時クリア
- UXの一貫性向上

## v1.3.0 更新内容（2026-06-04）

### P1 実装済み

#### 1. パターン比較グラフのスマートソート
- グラフ表示時にパターンを月間費用の降順（高い順）で自動ソート
- パターン追加順は変わらない（グラフ表示のみソート）
- 比較時に費用が高い順に表示されるため視認性向上

#### 2. 入力値バリデーション強化
- 月の外食回数（val2）の検証を追加
- 外食費・回数の両方が1以上の数値の場合のみ計算実行
- 条件を満たさない場合、明確なエラーメッセージを表示

#### 3. パターン入力キーボード操作改善
- パターン名 Enter キー → 金額フィールドに自動フォーカス
- 金額フィールド Enter キー → 月の回数フィールドに自動フォーカス
- 月の回数フィールド Enter キー → 削除ボタンに自動フォーカス
- キーボード入力体験を大幅改善

### P2 実装済み

#### 4. 結果コピーボタンの追加
- 結果エリア右側に「📋 コピー」ボタンを配置
- クリック時、「月間費用: XXX円, 年間費用: XXX円」をクリップボードにコピー
- コピー成功時、2秒間「✅ コピーしました！」メッセージを表示
- クリップボード機能非対応ブラウザの場合、エラーメッセージを表示

#### 5. パターン入力アクセシビリティ向上
- ラベルと入力フィールドを for/id 属性で紐づけ
- パターン削除ボタンに aria-label 属性を追加
- スクリーンリーダー対応を大幅強化
- キーボード操作の明確化

#### 6. パターン名プレースホルダー例の拡充
- パターン名プレースホルダー: 「ランチ、ディナー、カフェ等」
- ユーザが直感的に入力内容を理解しやすい
- 数値入力に step="1" 属性を追加（小数入力を防止）

## v1.2.0 更新内容（2026-06-03）

### P1 実装済み

#### 1. リセットボタンのスタイル改善 + フォーム検証
- リセットボタンを赤色（#ef4444）に変更して視認性向上
- 0円以下の値では計算を実行しない（バリデーション強化）
- ボタンのフォーカスアウトライン色を統一

#### 2. リアルタイム計算の debounce + トランジション効果
- 入力時の高頻度な再計算を debounce（200ms）で制限
- 結果表示・非表示時に fade-in/fade-out アニメーション（0.3s）
- `@keyframes fadeIn` でスムーズな表示

#### 3. 複数パターン比較の DOM 操作最適化
- `renderPatterns()` を効率化
- パターン削除機能の実装

### P2 実装済み

#### 4. ページリロード時のデータ永続化
- フォーム入力値と複数パターンデータを `localStorage` に保存
- ページロード時に自動復元（`loadData()` 関数）
- リセット時に `localStorage` も同時にクリア

#### 5. 複数パターン比較のグラフ可視化
- 各パターンの月間費用を CSS 棒グラフで表示
- 最大値に対する割合をバー幅で表現
- グラフ描画関数 `renderChart()` で動的更新

#### 6. モバイルレイアウト改善
- ボタンの高さを 48px に統一（タッチ対象）
- 小画面での結果表示フォント調整
- パディング・マージン最適化

#### 7. 年間費用表示の強調
- 結果エリアを grid レイアウトで再設計
- 月間費用（緑）と年間費用（紫）を並べて表示
- 各金額に明確なラベル付け

## v1.0.0 機能（初版）

- 基本的な外食費計算（1回あたり費用 × 月の回数）
- 年間費用計算
- 自炊との比較・節約額シミュレーション

## データ管理

- **ストレージ**: ブラウザ localStorage（外部API通信なし）
- **永続化**: ページ読み込み時に自動復元
- **クリア**: ブラウザのサイトデータ削除で初期化

## 技術スタック

- HTML5 / CSS3 / Vanilla JavaScript
- PWA対応（manifest.json / Service Worker）
- レスポンシブデザイン（モバイルファースト）

## 使い方

1. 入力フォームの各項目を確認する
2. 必要な数値・条件をすべて入力する
3. 計算ボタンをクリックして結果を取得する
4. 計算結果と詳細な内訳を確認する
5. 条件を変えて複数パターンを比較する

## よくある質問（FAQ）

**Q: 外食費用・予算管理ツールは無料で使えますか？**

はい、完全無料・登録不要でご利用いただけます。

**Q: 計算結果は正確ですか？**

参考値としてご活用ください。重要な判断の際は専門家・公式機関にご確認ください。

**Q: 何回でも計算できますか？**

はい、回数制限なく何度でもご利用いただけます。条件を変えて比較にお使いください。

**Q: 入力したデータはどこかに保存されますか？**

いいえ。すべての計算はブラウザ内で処理され、データのサーバー送信は行いません。

**Q: スマートフォンで利用できますか？**

はい、PC・スマートフォン・タブレットすべてに対応しています。


## 関連サービス

- [🍽️ 外食費・食費最適化診断](https://appadaycreator.github.io/eating-out-budget-advisor/)
- [Household Budget Analyzer](https://appadaycreator.github.io/household-budget-analyzer/)
- [週末まとめ調理プランナー](https://appadaycreator.github.io/meal-prep-planner/)

## テスト

| ファイル | フレームワーク | 概要 |
|---------|--------------|------|
| `tests/e2e/` | Playwright | 本番URL対象E2E（Jest対象外） |

## デプロイ

GitHub Pages（mainブランチ push → 自動デプロイ）

## ライセンス

MIT License
